type User {
  id: ID! @id
  userName: String @unique
  email: String! @unique
  password: String!
  refreshTokens: [Token!]!
}

type Token {
  id: ID! @id
  token: String! @unique
  loginId: String! @unique
  user: User! @relation(link: INLINE)
}


